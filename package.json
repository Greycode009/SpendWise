{
  "name": "spendwise",
  "version": "1.0.0",
  "private": true,
  "description": "SpendWise — offline-first personal finance Progressive Web App",
  "type": "module",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:deploy": "npm run db:deploy -w backend",
    "db:seed": "npm run db:seed -w backend",
    "test": "npm run test -w shared && npm run test -w backend && npm run test -w frontend",
    "test:e2e": "node e2e/run.mjs",
    "screenshots": "node e2e/screenshots.mjs"
  },
  "engines": {
    "node": ">=22.9"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "playwright": "1.56.1"
  }
}
